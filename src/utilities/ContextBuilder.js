/**
 * Takes a context object and a default context, and replaces
 * the default theme's values with the new themes values
 * *unless* the new theme does not have an entry, in which case
 * the default theme value is used
 * 
 * TODO: Implement this class
 */
class ContextBuilder {
  static overwriteDefaults(newContext, defaultContext) {
    returnTheme = {}
    for ( key in defaultContext) {
      
    }
  }
}
